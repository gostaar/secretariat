{% extends 'base.html.twig' %}

{% block title %}Dashboard utilisateur{% endblock %}

{% block body %}

<div class="body-user">
    <div class="container-fluid custom-bg p-0 m-0">
        <div class="container-user d-flex flex-column flex-md-row">
            <!-- Sidebar -->
            {% include 'partials/user/_headerTest.html.twig' %}

            <!-- Main Content -->
            <div class="col-md-9 p-4 d-flex flex-column">
                <!-- Factures Section -->
                {% include 'partials/user/_factures.html.twig' %}
                
                <!-- Agenda Section -->
                {% include 'partials/user/_agenda.html.twig' %}

                <!-- Documents Section -->
                {% include 'partials/user/_documents.html.twig' %}
                
                <!-- Acces Section -->
                {% include 'partials/user/_access.html.twig' %}

            </div>
        </div>
    </div>
    {% include 'partials/Page/footer.html.twig' %}
</div>
{% endblock %}
