<div class="sidebar-flex">
    <!-- Menu Grand Écran -->
    <div class="sidebar d-none d-md-block">
        <div class="text-center pb-4 pt-4">
            <a href="{{ path('app_logout') }}" id='homeButton'><h2>Secrétaire Pro<h2></a>
        </div>
        <div class="d-flex align-items-center mb-2 px-3 py-3">
            <a href="{{ path('user_profile') }}" class="flex-1">
                <img src="{{ asset('build/images/avatar.png') }}" alt="Avatar" class="icon-size" />
            </a>
            <ul class="flex-2-ul">
                <li class="list-unstyled text-success">{{ app.user.email }}</li>
                <li class="list-unstyled">
                    <a href="{{ path('app_logout') }}" class="bt w-100 text-danger" id="logoutButton">Déconnexion</a>
                </li>
            </ul>
        </div>
        <ul class="list-unstyled p-3">
            {% include 'partials/user/_headerMenus.html.twig' %}
        </ul>
    </div>

    <!-- Menu Burger (Petits Écrans) -->
    <nav class="sidebar d-md-none navbar-expand-md fixed-top" id="navBurger">
        <div class="container-fluid">
            <button class="menu-toggle" id="menu-toggle"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation"
            >&#9776;</button> 
            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="text-center pb-4 pt-4">
                    <h2><a href="{{ path('app_logout') }}" class="text-primary" >Secrétaire Pro</a></h2>
                </div>
                <div class="user-avatar d-flex mb-4 align-items-center">
                    <a href="{{ path('user_profile') }}" style="flex:1;" class="d-flex justify-content-center">
                        <img src="{{ asset('build/images/avatar.png') }}" alt="Avatar" class="icon-size"/>
                    </a>
                    <ul class="flex-2-ul p-0">
                        <li class="list-unstyled text-success">{{ app.user.email }}</li>
                        <li class="list-unstyled">
                            <a href="{{ path('app_logout') }}" class="bt w-100 text-danger">Déconnexion</a>
                        </li>
                    </ul>
                </div>
                <ul class="sidebar-nav list-unstyled pt-0">
                    {% include 'partials/user/_headerMenus.html.twig' %}
                </ul>
            </div>
        </div>
    </nav>
</div>
